import { describe,expect,it } from 'vitest';import { applyChoice,initialGameState,resetGame } from '../state/gameReducer';
describe('applyChoice',()=>{it('applies compassion outcome',()=>{const s=applyChoice(initialGameState,'compassion');expect(s.title).toBe('PROTECTOR OF THE FORGOTTEN');expect(s.traits.compassion).toBe(4);expect(s.selectedChoice).toBe('compassion')});it('applies order outcome',()=>{const s=applyChoice(initialGameState,'order');expect(s.title).toBe('KEEPER OF ORDER');expect(s.traits.courage).toBe(3);expect(s.traits.wisdom).toBe(1)});it('applies punishment outcome',()=>{const s=applyChoice(initialGameState,'punishment');expect(s.title).toBe('THE IRON HAND');expect(s.traits.ambition).toBe(3)});it('resets to initial state',()=>expect(resetGame()).toEqual(initialGameState))});
