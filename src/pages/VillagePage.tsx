import { useNavigate } from '../app/router';import { AppFrame } from '../components/AppFrame';import { PixelScene } from '../components/PixelScene';import { ReputationPanel } from '../components/ReputationPanel';import { useGame } from '../state/GameStateContext';
export function VillagePage(){const nav=useNavigate();const {state,reset}=useGame();return <AppFrame><header className="hero"><p className="eyebrow">THE VILLAGE OF</p><h1>ASHFALL</h1></header><PixelScene kind="village" choice={state.selectedChoice}/><ReputationPanel state={state}/><button className="primary" onClick={()=>nav('/trial')}>FACE TODAY’S TRIAL</button><button className="reset" onClick={reset}>Reset Story</button></AppFrame>}
