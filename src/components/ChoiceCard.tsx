import { PixelSprite } from './PixelSprite';import { PixelSpriteData } from '../types/pixel';
export function ChoiceCard({label,copy,icon,theme,disabled,selected,dimmed,onClick}:{label:string;copy:string;icon:PixelSpriteData;theme:string;disabled:boolean;selected:boolean;dimmed:boolean;onClick:()=>void}){return <button className={`choice ${theme} ${selected?'selected':''} ${dimmed?'dimmed':''}`} disabled={disabled} onClick={onClick}><svg viewBox="0 0 40 40" className="choiceIcon"><PixelSprite sprite={icon} pixelSize={4} x={1} y={4}/></svg><span><strong>{label}</strong><em>{copy}</em></span></button>}
