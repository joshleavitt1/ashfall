import { PixelSprite } from './PixelSprite';import { gemEmpty,gemFilled,heartIcon,shieldIcon,crownIcon } from '../pixel/icons';
const icons={compassion:heartIcon,courage:shieldIcon,ambition:crownIcon};const labels={compassion:'Compassion',courage:'Courage',ambition:'Ambition'};
export function TraitMeter({trait,value}:{trait:keyof typeof icons;value:number}){return <div className={`trait ${trait}`}><svg viewBox="0 0 28 28" className="traitIcon"><PixelSprite sprite={icons[trait]} pixelSize={3} x={2} y={3}/></svg><span>{labels[trait]}</span><svg viewBox="0 0 105 18" className="gems" aria-label={`${labels[trait]} ${value} out of 5`}>{Array.from({length:5},(_,i)=><PixelSprite key={i} sprite={i<value?gemFilled:gemEmpty} pixelSize={3} x={i*20+2} y={1} paletteOverrides={i<value?{G:'currentColor',L:'currentColor',D:'currentColor'}:{}} />)}</svg></div>}
